//current user
// and is logged in value, or it's own file

// basic order follows last sprint,
// make sure registration works
// then log in
// after logging in make sure user info is displaying correctly, implement the token thing, that can be last

import React from "react";

const CurrentUserContext = React.createContext();

export default CurrentUserContext;
